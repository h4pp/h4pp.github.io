title: h4pp
description: Apps by h4pp
email: h4pp@proton.me
theme: minima

plugins:
  - jekyll-feed

# Header nav is removed via _includes/header.html (brand only) — these
# pages are deep-linked from the App Store, not browsed.

exclude:
  - Gemfile
  - Gemfile.lock
  - README.md
